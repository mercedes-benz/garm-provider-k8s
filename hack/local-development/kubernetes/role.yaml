apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: garm-provider-k8s
  namespace: runner
rules:
  # Create/delete runner pods and read/list them for instance status.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "create", "delete"]
